# Backend base URL — no trailing slash, no /api suffix.
# Leave empty in local dev (Vite proxy handles /api → localhost:3001).
# Set to your Render service URL in production, e.g.:
#   VITE_API_URL=https://veeyds-api.onrender.com
VITE_API_URL=
